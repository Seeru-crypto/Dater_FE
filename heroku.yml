build:
  config:
    PORT: development
    REACT_APP_BACK_END_URL: ${REACT_APP_BACK_END_URL}
    REACT_APP_SWAGGER_LINK: bar
    REACT_APP_MAIL_EXAMPLE: bar
    REACT_APP_TOKEN_API_PATH: bar
  docker:
    web: Dockerfile
